<section class="case-studies-section">
  <div class="container">
    <h3 class="case-studies__suptitle">{{ section.settings.suptitle }}</h3>
    <h2 class="case-studies__title">{{ section.settings.title }}</h2>
    <div class="case-studies__studies js-case-studies-slider">
      {% for block in section.blocks %}
        <div class="case-studies__study">
          <div class="row">
            <div class="col-md-5 flex-col-wrap justify-content-center">
              <div class="case-studies__study-content">
                <h2 class="case-studies__study-title">{{ block.settings.title }}</h2>
                <div class="case-studies__study-text">{{ block.settings.description }}</div>
              </div>
            </div>
            {% if block.settings.image_before and block.settings.image_after %}
              {%- include 'image-compare' 
                with before: block.settings.image_before, 
                after: block.settings.image_after, 
                initial: 50, 
                indicators: true
              -%}
            {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
  {
    "name": "Case Studies",
    "settings": [
      {
        "type": "text",
        "id": "suptitle",
        "label": "Suptitle"
      },
      {
        "type": "textarea",
        "id": "title",
        "label": "Title"
      }
    ],
    "blocks": [
      {
        "type": "study",
        "name": "Case Study",
        "settings": [
          {
            "type": "textarea",
            "id": "title",
            "label": "Study title"
          },
          {
            "type": "textarea",
            "id": "description",
            "label": "Study description"
          },
          {
            "type": "image_picker",
            "id": "image_before",
            "label": "Before image"
          },
          {
            "type": "image_picker",
            "id": "image_after",
            "label": "After image"
          }
        ]
      }
    ],
    "presets": [{
        "name": "Case Studies",
        "category": "Front Page"
    }]
  }
{% endschema %}
